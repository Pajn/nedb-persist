{
  "name": "nedb-persist",
  "version": "0.1.0",
  "description": "A NeDb storage for redux-persist",
  "main": "lib/index.js",
  "scripts": {
    "prepublish": "babel src --out-dir lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rasmus Eneman <rasmus@eneman.eu>",
  "license": "Apache-2.0",
  "dependencies": {
    "nedb": "^1.6.0"
  },
  "devDependencies": {
    "babel": "^6.3.26",
    "babel-cli": "^6.4.0",
    "babel-preset-es2015": "^6.3.13",
    "babel-preset-stage-2": "^6.3.13"
  }
}
